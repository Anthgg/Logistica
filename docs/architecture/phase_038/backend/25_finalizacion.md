# 25. Finalización

Finalizar requiere operación en progreso, sin pausa activa y todos los checks de cierre obligatorios aprobados. El servidor fija finalización y calcula `gross_duration_seconds`, `total_pause_seconds` y `net_duration_seconds`, rechazando secuencias negativas.

