# 35. Integración con Fase 037

La única entrada funcional es `DockAssignmentPreparationService.get_preparation`, invocada de forma read-only. Solo acepta `ENTRY_AUTHORIZED` o `ENTRY_AUTHORIZED_WITH_OBSERVATIONS`; no modifica Gate ni duplica sus verificaciones.

